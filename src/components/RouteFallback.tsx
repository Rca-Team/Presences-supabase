import React from 'react';

/**
 * RouteFallback — Invisible, zero-glitch route fallback
 * Completely eliminates intrusive loading animations, layout-shifting boxes,
 * and flickering laser progress bars during route chunk loading.
 */
const RouteFallback: React.FC = () => {
  return null;
};

export default RouteFallback;
